//분명 다르지 않게 나오는데 백준에서는 틀린 답 처리 됨
#include <stdio.h>
int main()
{

  int H, M;
  scanf("%d %d", &H, &M);

  if(M>=45)
  {
    printf("%d %d", H, M - 45);

  }
  else
  {
    if(H>1)
    {
    printf("%d %d", H - 1, 15 + M);
    }
    else
    {
      printf("%d %d", 23, 15 + M);
    }
  }
   
}
