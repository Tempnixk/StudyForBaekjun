#include <stdio.h>
int main()
{
    int N, i, j;
    scanf("%d", &N);

    for(j=1; j<2; j++)
    {
      for(i=1; i<10; i++)
      {
          printf("%d * %d = %d\n", N, i, i * N);
      }
    }
    return 0;
}
